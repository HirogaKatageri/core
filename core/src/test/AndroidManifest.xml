<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.hirogakatageri.core">

    <application
        android:name=".components.TestApp"
        android:theme="@style/Theme.Core">

        <activity android:name=".components.TestActivity" />
        <activity android:name=".components.TestViewModelActivity" />

    </application>

</manifest>